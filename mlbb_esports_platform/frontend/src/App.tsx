// NOTE: App.tsx is legacy/unused.
// The running app entry is `frontend/src/main.tsx` which mounts `AppFixed`.
// Keep this file as a safe re-export to avoid breaking any imports.

import AppFixed from './AppFixed';

export default AppFixed;

